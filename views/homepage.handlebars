{{!-- <ol class="post-list">
    {{#each posts as |post|}}
    <li class="list">
        {{> post-info post }}
    </li>
    {{/each}}
</ol> --}}

<div class="container">
    <div class="row, post-home">
        <div class="col-12 align-self-start">
            <ol class="post-list">
                {{#each posts as |post|}}
                <li class="list">
                    {{> post-info post }}
                </li>
                {{/each}}
            </ol>
        </div>
    </div>
</div>